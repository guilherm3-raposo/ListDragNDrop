{
	"name": "listdragndrop",
	"version": "1.0.0",
	"description": "",
	"main": "server/server.js",
	"scripts": {
		"prod": "rm -r dist/* && cp src/templates/* dist && cp src/css/* dist && browserify lib/angular.js -o bundle.js && browserify src/js/app.js -o -e dist/app.js && node server/server-prod.js",
		"bundle": "browserify src/js/app.js -o dist/bundle.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"bower": "^1.8.8",
		"colors": "^1.4.0",
		"express": "^4.18.2"
	},
	"devDependencies": {}
}
